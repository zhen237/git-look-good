# Git 提交图谱工具 · 需求文档（PRD v1.0）

> 状态：v1.0（需求定稿，进入开发）
> 生成时间：2026-08-17
> 定位：本地优先的个人 Git 可视化工具（单文件、双击即用）

**需求已定稿**：① 时间轴叠加（FR-7）；② diff 采用**完整档**（FR-9，点击提交可展开逐行代码 diff）；③ 统计面板（FR-8）；④ 配色定为 **A. 活泼彩色**；⑤ 运行形态为单文件双击即用。下文进入开发，先做 M1 数据层验证。

---

## 1. 项目背景

灵感来源：想把团队的 Git 提交历史做成一个**可交互的横向树状图谱**，直观看到"谁、在哪个仓库、哪个分支、什么时间、提交了什么"。当前 Git 客户端（命令行、GitKraken、SourceTree 等）要么偏线性时间线、要么偏分支拓扑，缺少一个**以"人 + 仓库"为主轴、又能展开到分支与提交**的聚合视图。

本工具不追求替代专业 Git 客户端，而是做一个**轻量、本地、好看、能快速理解代码结构演进**的"图谱"。

---

## 2. 目标与价值

- **一眼看懂结构**：以树状方式呈现「仓库 → 分支 → 作者 → 提交」，比纯日志直观得多。
- **适配任意仓库**：工具不绑定特定项目，指向任意标准 Git 仓库即可扫描，无需改造仓库。
- **多维信息共存**：仓库、分支、作者、提交、时间五个维度都在图谱中可查可见。
- **本地优先、隐私安全**：数据只在本机读取与渲染，绝不上传。

---

## 3. 用户与使用场景

| 项 | 内容 |
|---|---|
| 用户角色 | 开发者个人 / 技术负责人，自用 |
| 使用场景 | 本机双击运行，分析自己机器上的仓库（个人工具，无需部署、无需安装环境） |
| 典型动作 | 双击打开 → 选择/书签一个仓库 → 扫描 → 浏览图谱（含时间轴）→ 筛选/搜索 → 看某次提交详情（含 diff）→ 切到统计面板 |
| 非目标 | 团队协作平台、多人权限、云端部署（本期不做） |

---

## 4. 核心功能需求（FR）

### FR-1 仓库适配与扫描
- 支持选择本机任意目录作为 Git 仓库根（自动识别 `.git`）。
- 一键扫描，解析 `git log` 提取全部提交记录（含作者、邮箱、时间、分支、消息、改动文件数、增删行数、逐文件 diff 数据）。
- 支持把常用仓库加入「书签列表」，下次直接打开。

### FR-2 横向树状图谱（核心）
- 以**横向树状图**（根在左、向右展开）呈现提交结构。
- 节点可**展开 / 折叠**，默认折叠到合适层级，避免大仓库卡顿。
- 支持**切换分组维度（根维度）**：
  - 模式 A（默认）：仓库 → 分支 → 作者 → 提交（时间排序）
  - 模式 B：仓库 → 作者 → 提交（分支作为属性/标签）
  - 模式 C：作者 → 仓库 → 分支 → 提交（团队视角）
  - 模式 D：时间线模式（以时间为横轴铺开，弱化严格的树层级）

### FR-3 维度信息完整呈现
图谱中每个节点携带并展示：
- **仓库节点**：仓库名、分支数、提交总数、贡献者数。
- **分支节点**：分支名、该分支提交数、最近提交时间。
- **作者节点**：作者名/头像色、提交数、活跃时间段。
- **提交节点**：短 hash、提交信息、时间、改动文件数、所属分支标签。

### FR-4 交互能力
- **悬停（hover）**：显示该节点关键信息摘要。
- **点击提交节点**：右侧/弹层展示完整 commit 详情（消息、作者、时间、文件清单、增删行数；可展开逐行 diff，见 FR-9）。
- **筛选**：按作者、分支、时间范围过滤图谱。
- **搜索**：按提交信息 / hash / 作者名 全局搜索并高亮定位。
- **配色**：每个作者固定一种颜色（基准为 A. 活泼彩色），跨分支/仓库保持一致。

### FR-5 数据导出
- 导出当前视图为 **JSON**（结构化数据）与 **PNG/SVG**（图谱图片），便于分享或留存。

### FR-6 配置与偏好
- 记住书签仓库、上次使用的分组模式、作者配色、窗口尺寸等（本地配置文件）。

### FR-7 时间轴叠加
- 在横向树状图上方/背景叠加**时间刻度轴**（年-月），节点按提交时间对齐到对应位置。
- 支持缩放时间轴（日 / 月 / 年粒度），便于在大时间跨度下定位活跃期。
- 与树层级共存：时间轴作为"横向坐标参考"，不破坏树的分组结构。

### FR-8 统计面板（补充视图）
- 作为图谱旁边的补充视图，提供量化洞察：
  - **作者排行**：每人提交数、增删行数 Top N（柱状/环形）。
  - **活跃热力**：按日/周的提交密度热力图。
  - **仓库概览**：各仓库提交总数、分支数、贡献者数。
  - **趋势**：提交量随时间的变化曲线。
- 统计面板与图谱联动：在面板中点击某作者/时间段，图谱同步高亮筛选。

### FR-9 提交 diff 详情（完整档 · 已定）
- 点击提交节点后，详情层**默认展示文件清单 + 增删行数**，并可**展开查看逐行代码 diff**（红=删除行，绿=新增行）。
- 大改动提供折叠/分页，避免刷屏。
- 数据层需为每次提交保留逐文件 added/deleted 行数（用于 diff 与统计）。

---

## 5. 数据模型

```
Repo        { id, name, path, branches[], authors[], commitCount }
Branch      { name, commitCount, lastCommitTime }
Author      { id, name, email, color, commitCount, activeStart, activeEnd }
Commit      { hash, authorId, branch, message, time, filesChanged, additions, deletions, parents[], files[{path, additions, deletions}] }
```

数据来源：本地运行 `git log --all --source --pretty=format:... --numstat` 解析为上述结构，缓存到本地 JSON。

---

## 6. 横向树状图设计（重点）

**布局**：从左到右（横向），根节点在左，逐级向右展开；节点用连线表示父子关系；同一层级纵向排列。

**默认层级（模式 A）**：
```
[全部仓库]
   └─ 仓库A
         └─ main 分支
               └─ 作者 张三
                     ├─ commit abc123 (2026-08-10)
                     ├─ commit def456 (2026-08-12)
               └─ 作者 李四
                     ├─ commit ...
         └─ dev 分支
               └─ 作者 张三
                     ├─ ...
```

**视觉元素**：
- 作者用固定颜色区分（基准为 A. 活泼彩色，见第 12 节）。
- 提交节点大小可反映改动文件数 / 增删行数。
- 折叠分支/作者节点以控制信息密度。
- 时间以节点排序 + 顶部时间轴刻度（FR-7）呈现。

---

## 7. 技术架构（由我定方案）

| 层 | 选型（推荐） | 说明 |
|---|---|---|
| 解析引擎 | Node.js / Go（解析 `git log`、计算统计、生成数据） | 随程序打包，用户无需单独安装 |
| 前端 | Vue 3 + D3.js（或类似树图库） | 负责横向树渲染、时间轴、统计面板与交互 |
| 运行形态 | **单文件可执行程序（双击即用）** | 打包为一个 `.exe`/`.app`，双击打开窗口即用，数据不出本机 |
| 存储 | 本地 JSON 文件（仓库缓存 + 用户配置） | 无需数据库，随程序目录或用户目录存放 |

> 纯浏览器 HTML 因沙箱限制无法直接读取本机 `.git`，故采用「单文件桌面程序」方案——把解析引擎 + 界面打包进一个可执行文件，双击即开。推荐 Electron（Web 技术栈）或 Go + 内嵌 WebView。

---

## 8. 非功能需求

- **性能**：单仓库数万提交应能流畅扫描与渲染（采用懒加载/折叠 + 虚拟化）。
- **隐私**：所有数据仅在本机处理，不联网、不上传。
- **跨平台**：Windows / macOS / Linux 均可运行（以 Windows 双击 `.exe` 为首要目标）。
- **易用**：首次打开有引导，指向仓库即可用。

---

## 9. 开发里程碑

- **M1 数据层（已完成验证）**：仓库识别 + `git log` 解析 + 本地缓存（含逐文件 diff 数据、精确分支归属、作者配色）。✅ 验证通过
- **M2 图谱渲染**：横向树状图（默认模式 A）+ 时间轴叠加 + 展开折叠 + 作者配色。
- **M3 交互层**：hover/点击详情（含 diff）、筛选、搜索、分组模式切换、统计面板。
- **M4 打包打磨**：打包为单文件可执行、书签、导出、配置持久化、性能优化、引导页。

---

## 10. 风险与待定

- **大仓库性能**：超大型仓库（10 万+ 提交）的渲染需做虚拟化或抽样。
- **合并提交展示**：merge commit 在树中如何表达（作为普通节点还是特殊连线）待定。
- **作者去重**：同一人多个邮箱 / 改名时的合并策略待定。

---

## 11. 遗留/可选问题（非阻塞）

1. （可选）统计面板除作者排行/热力/趋势外，还想要哪些指标？
2. （待定）merge commit 在树中如何表达；作者多邮箱去重策略。

> 需求已定稿（v1.0），进入开发。M1 数据层验证进行中。

---

## 12. 配色定稿：A. 活泼彩色

已选 **A. 活泼彩色** 作为作者配色基准（高区分度，适合作者多的团队）。调色板：

蓝 `#4E79A7`、橙 `#F28E2B`、绿 `#59A14F`、红 `#E15759`、紫 `#B07AA1`、青 `#76B7B2`、黄 `#EDC948`、粉 `#FF9DA7`，作者按出现顺序循环取色。

> 预览见随文档的可视化配色图（已在需求讨论中以可视化形式展示）。
