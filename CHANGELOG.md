# 更新日志

## v2.1.5 (2026-08-18)

- **修复（关键）**：`build.js` 向 `__DATA__` 占位符嵌入 JSON 时改用 replacer 函数，避免数据中的 `$&` / `` $` `` / `$'` / `$1~$9` 被 `String.replace` 当作特殊替换标记解释，导致 `git-graph.html` / 桌面 exe 在扫描含 `$` 的仓库时出现 `JSON.parse` 控制字符错误与整文件 JS 语法损坏。
- **P0 补齐**：打开时自动适配视图；内联切换仓库 / 刷新按钮；全量 diff 扫描进度提示。
- **P1 补齐**：导出 SVG / PNG / JSON；`localStorage` 偏好持久化；作者多邮箱去重；时间轴粒度切换。
- **P2 补齐**：快捷键 / 帮助浮层；大仓库采样上限；章鱼合并泳道校验。
- 桌面端重新组装 `dist_v215`（`GitGraph.exe`，348MB，含全部修复）；清理过期 `dist` / `dist_v212` / `dist_v214`（回收约 2.4GB）。

## v2.1.4

- 初始稳定版：单文件 HTML 图谱 + Electron 桌面壳，支持分支泳道、作者过滤、时间轴缩放。
